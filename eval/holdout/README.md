# Preference Interpreter Holdout Eval

This directory is reserved for future holdout preference interpreter cases.

Do not generate answers for these cases during development. Do not derive these cases from the current 20-case development/regression set in `eval/test_case.json`.

Holdout cases should be prepared separately and treated as unseen evaluation data.

